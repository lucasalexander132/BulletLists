import { HeartIconDirective } from './heart-icon.directive';

describe('HeartIconDirective', () => {
  it('should create an instance', () => {
    const directive = new HeartIconDirective();
    expect(directive).toBeTruthy();
  });
});
